# Google Calendar Integration
# Required: Your Google OAuth 2.0 Client ID
# To obtain one, follow these steps:
# 1. Go to Google Cloud Console: https://console.cloud.google.com/
# 2. Create a new project or select an existing one
# 3. Enable the Google Calendar API
# 4. Go to "Credentials" and create an OAuth 2.0 Client ID
# 5. Set the application type to "Web application"
# 6. Add authorized JavaScript origins (e.g., http://localhost:5173 for development)
# 7. Add authorized redirect URIs (e.g., http://localhost:5173/oauth/callback)
# 8. Copy the Client ID and paste it below
VITE_GOOGLE_CLIENT_ID=

# Optional: Custom OAuth redirect URI
# Defaults to ${window.location.origin}/oauth/callback if not set
# VITE_GOOGLE_REDIRECT_URI=http://localhost:5173/oauth/callback
